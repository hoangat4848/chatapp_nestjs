export enum Routes {
  AUTH = 'auth',
  USERS = 'users',
  CONVERSATIONS = 'conversations',
}

export enum Services {
  AUTH = 'AUTH_SERVICE',
  USERS = 'USER_SERVICE',
  CONVERSATIONS = 'CONVERSATION_SERVICE',
}
